import 'package:flutter/material.dart';

class ControllerListScreen extends StatelessWidget {
  const ControllerListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
